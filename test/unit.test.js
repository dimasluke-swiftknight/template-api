/* eslint-disable no-undef */
const assert = require('assert');

describe('Unit Test', () => {
  describe('Passing', () => {
    it('Template test should Pass', () => {
      assert.equal(1 + 1, 2);
    });
  });
});
